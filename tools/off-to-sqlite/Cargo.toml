[package]
name = "off-to-sqlite"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rusqlite = { version = "0.35", features = ["bundled"] }

[profile.release]
opt-level = 3
lto = true
